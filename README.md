# yofun

Современное веб-приложение для поиска и просмотра городских мероприятий в Йошкар-Оле. Концерты, выставки, квизы, театральные постановки и многое другое в одном месте.

## 🚀 Особенности

- **Современный дизайн** - Красивый интерфейс с градиентами и анимациями
- **Адаптивность** - Отлично работает на всех устройствах
- **Быстрая загрузка** - Оптимизировано для максимальной производительности
- **Интерактивность** - Динамические карточки событий с дополнительной информацией
- **Формы обратной связи** - Валидация и отправка данных на внешний API
- **Подписка на новости** - Система подписки на уведомления о новых событиях

## 🛠 Технологии

- **Frontend**: Next.js 14, React 18, TypeScript
- **Стилизация**: Tailwind CSS, shadcn/ui
- **Иконки**: Lucide React
- **Контейнеризация**: Docker, Docker Compose
- **Развертывание**: Production-ready Docker образы

## 📋 Требования

- Node.js 18+
- npm 8+
- Docker (для контейнеризации)
- Docker Compose (для оркестрации)

## 🚀 Быстрый старт

### Локальная разработка на Docker Compose

1. **Клонируйте репозиторий**
   ```bash
   git clone https://gitlab.com/yo3395005/yofun.git
   cd yofun
   ```

3. **Запустите контейнеры в режиме разработки**
   ```bash
    docker compose -f docker-compose.dev.yml up -d 
   ```

4. **Откройте в браузере**
   ```
   http://localhost:3000
   ```

## 🐳 Продакшен с Docker

### Сборка и запуск

1. **Соберите и запустите продакшен контейнеры**
   ```bash
   npm run docker:prod
   ```

2. **Приложение будет доступно по адресу**
   ```
   http://localhost:3000
   ```

# Пересобрать образы (dev-контейнеры)
```
docker compose -f docker-compose.dev.yml up --build -d
```

### Ручная сборка Docker образа

# Собрать образ
```
docker compose -f docker-compose.dev.yml build
```

## 📁 Структура проекта

```
yofun/
├── docker/                    # Докер-конфиги
├── src/                       # Исходный код проекта
├── docker-compose.prod.yml    # Prod - конфигурация
├── docker-compose.dev.yml     # Dev - конфигурация
└── README.md                  # Документация
```
Более подробное описание структуры проекта выложу чуть позжу.

## 🔧 Конфигурация

### Переменные окружения

Для frontend-части приложения используется свой файл окружения, для backend-части - свой файл. Описание этих файлов 
находятся соответствующих каталогах

## 📝 API Endpoints

Приложение предоставляет следующие API endpoints:

- `GET /api/v1/index` - Получение данных для главной страницы
- `GET /api/v1/quizzes` - Получение всех актуальных квизов
- `GET /api/v1/events` - Получение всех актуальных мероприятий
- `GET /api/v1/static` - Получение данных для header и footer
- `POST /api/v1/feedback` - Отправка формы обратной связи

Эти endpoints работают как прокси для внешнего API сервера.

<b>ВАЖНО:</b> дабы избежать путаницы в наименованиях, мероприятия и квизы являются частью другой общей сущности - 
<u>события</u>. Событие имеется в виду не системное событие, а именно сущность, которую предоставляет
проект (карточки квизов и мероприятий).

## 🎨 Кастомизация

### Цветовая схема

Основные цвета определены в `tailwind.config.ts`:
- Фиолетовый: `purple-600`, `purple-700`
- Розовый: `pink-500`, `pink-600`
- Индиго: `indigo-950`

### Компоненты

Все UI компоненты находятся в папке `components/ui/` и основаны на shadcn/ui и других библиотеках.

## 🚀 Развертывание в prod-окружении

### Docker в продакшене

1. Клонируйте репозиторий на сервер:
```bash
  git clone https://gitlab.com/yo3395005/yofun.git
  cd yofun
```
2. Соберите контейнеры и запустите их:
```bash
  docker compose -f docker-compose.prod.yml build
  docker compose -f docker-compose.prod.yml up -d
```

3. Разверните Laravel:
```bash
  # Устанавливаем composer-зависимости в контейнере (ставим сам Laravel)
  docker compose -f docker-compose.prod.yml exec api composer install
  
  # Далее устанавливаем миграции, генерим ключ шифрования и открываем доступ на просмотр статических файлов
  docker compose -f docker-compose.prod.yml php artisan migrate
  docker compose -f docker-compose.prod.yml php artisan key:generate
  docker compose -f docker-compose.prod.yml php artisan storage:link
  docker compose -f docker-compose.prod.yml php artisan sources:init
 
```
4. Настройте переменные окружения `src/frontend/.env` и `src/backend/.env` в соответствии с .env.example

5. Радуемся, если проект заработал🥳 / Грустим, если нет😭 - но все равно параллельно пытаемся его запустить. В крайнем
случае, всегда можно написать команде разработки.
6. Теперь проект точно должен заработать!

<b>P.S:</b> эту инструкцию когда нибудь нужно вынести в отдельный файл, + так же создать отдельный файл с более 
подробным описанием проекта - например, написать про админку, свагер, самописные artisan-команды и про другие 
технологии, когда они подключатся.


## 📄 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](LICENSE) для деталей.
Чуть позже добавлю

## 🆘 Поддержка

Если у вас возникли вопросы или проблемы:

Свяжитесь с командой разработки!

## 📊 Статус проекта

- ✅ Основной функционал
- ✅ Адаптивный дизайн
- ✅ Формы с валидацией
- ✅ Docker контейнеризация
- 🔄 Интеграция с внешним API
- 🔄 Тестирование
- 📋 Документация API

----
# Задачи на ближайшее время
Для заметок (и примерные планы на будущее. Планы более высокоуровневые, нежели просто
написание кода).

1. 1. [ ] Выложить проект на VPS-сервер
2. 2. [ ] Vercel. Погуглить что это и как работает
3. 3. [ ] GitLab actions. Настроить пайплайны
4. 4. [ ] Подключить Яндекс-метрику в купе с Вебмастером 
5. 5. [ ] Подключить микроразметку 
6. 6. [ ] Поизучать k8s и попробовать воткнуть его в проект (или проект в него)

- После того, как все эти пункты будут сделаны, можно уйти в свободное плавание - рефакторить, добавлять
новые фичи, новые технологии и тому подобное. А пока - попытаться реализовать пункты из этого списка

#### В процессе выполнения этих шагов буду пытаться отмечать их прогресс здесь с кратким описанием внизу. 

---
# История версий
v1.0 (06.08.2025). Первый релиз, этакое MVP

---

Сделано с ❤️ командой yofun (макет накидала нейронка)
